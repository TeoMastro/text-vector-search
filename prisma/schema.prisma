generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgresql"
    url      = env("POSTGRES_PRISMA_URL")
}

model TextEmbedding {
    id        Int     @id @default(autoincrement())
    text      String
    embedding Float[]
}
